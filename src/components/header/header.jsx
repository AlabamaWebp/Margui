import React from 'react';

const Header = (
    floor,
    setFloor,
    cabinet,
    setСabinet,
) => {
    return (
        <header className='mainHeader'>
        </header>
    )
}
export default Header;